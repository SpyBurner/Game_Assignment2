#ifndef SDL_CUSTOM_EVENT_HPP
#define SDL_CUSTOM_EVENT_HPP

#include <SDL2/SDL.h>

const Uint32 SDL_GOAL1_EVENT_TYPE = SDL_RegisterEvents(1);
const Uint32 SDL_GOAL2_EVENT_TYPE = SDL_RegisterEvents(1);

#endif